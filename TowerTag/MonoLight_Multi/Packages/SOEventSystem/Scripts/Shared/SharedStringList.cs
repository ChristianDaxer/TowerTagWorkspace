using UnityEngine;

namespace SOEventSystem.Shared {
	[CreateAssetMenu(menuName = "Shared/List<string>", fileName = "Wow, much strings, so many!", order = 100)]
	public class SharedStringList : SharedList<string> {}
}
